# Database
DB_URL=jdbc:postgresql://localhost:5432/metalops_auth
DB_USER=your_username
DB_PASS=your_password

# JWT (coincide con application.properties)
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRATION=86400000

# CORS (coma-separado)
APP_CORS_ORIGINS=http://localhost:3000

# Optional: Spring Boot server port
# server.port=8080
